 
if not timerexists "shroomy"
    createtimer "shroomy"
    settimer "shroomy" 61000
endif

while skill "magery" < 100
    if skill "magery" < 100
        if mana > 49 
            say "an air elemental release"
            wait 300
            cast "air elemental"
            pause 5500
            menu 0x4083AF65 1
        endif
    endif    
    #Eat a magic mushroom if we have 
    #25  less mana than full
    if mana < 48 and skill 'Magery' > 1 
        if timer "shroomy" >= 61000
            dclicktype '29012'
            overhead 'Mushroom Power!'
            settimer "shroomy" 0
        endif
        skill 'meditation'
        wait 3000
    endif
endwhile
    