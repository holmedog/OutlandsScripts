while skill 'Magery' < 100
    cast 'Earth Elemental'
    wait 6000
    while findtype 'an Earth Elemental' as found
        cast 'Dispel'
        wft 
        targettype 'an Earth Elemental'
        wait 500
    endwhile
    #create shrooms
    #while counttype 29012 backpack < 5
    #    cast "create food"
    #    wait 1000
    #endwhile

    #if mana < 70
    #    dclicktype 29012
    #endif

    if findtype 'storage shelf' ground any any 2 as found
        menu found 1
    endif
    
    if mana < 55
        while mana < 95 and skill 'Magery' > 1 
            if not findbuff "Actively Meditating"
                skill 'meditation'
                wait 300
            endif
            wait 200
        endwhile
    endif
endwhile