@createlist packAnimals

@clearlist packAnimals
@pushlist 'packAnimals' "p1"
    
if list packAnimals > 0
    overhead 'Yo!'
else 
    overhead 'WTF!'
endif

foreach x in packAnimals
  overhead  x
endfor
wait 1000
@poplist 'packAnimals' 'front'

    
if list packAnimals > 0
    overhead 'Yo!'
else 
    overhead 'WTF!'
endif
wait 500
foreach x in packAnimals
  overhead  x
endfor