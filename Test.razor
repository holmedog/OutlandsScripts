if not timerexists 'throwNet'
    createtimer 'throwNet'
    settimer 'throwNet' 9999
endif

if timer 'throwNet' > 3500 and findtype basket|18824|swordfish|rocks|bedroll ground as fishingSpot
    dclicktype 3530 self
    while not targetexists  
        wait 20
    endwhile
    if targetexists 'Neutral'
        target fishingSpot
        settimer 'throwNet' 0
    endif
endif

replay