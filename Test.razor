if skill "Necromancy" > 80 
    overhead "{{boardState}}"
        #painspike
        if 'boardState' = 1
            overhead "Here1"
            if findtype 'corpse' ground any any 10 as found
                say '[painspike'
                wft 2500
                target found
                wait 350
            endif    
        endif
        #Vamp 
        else 
            overhead "Here2"
            if findtype 'corpse' ground any any 10 as found
                say '[vampiricembrace'
                wft 2500
                target found
                wait 350
            endif    
        endif
    endif  