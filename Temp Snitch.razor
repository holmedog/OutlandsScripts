createtimer 'hiding'
settimer 'hiding' 20000

createtimer 'spam message'
settimer 'spam message' 20000

createtimer 'spam close message'
settimer 'spam message' 20000
guild 'Town Guard is turned ON'
while not dead 
    # If we are not hidden, dont do anything until hidden.
    while not hidden
        if timer 'hiding' > 10500
            hotkey 'Hiding'
            settimer 'hiding' 0
        endif
    endwhile
    
    # Tracking
    if insysmsg 'Now tracking:'
        if timer 'spam message' > 5000
        guild 'Someone by idoc'
            wait 3000

            settimer 'spam message' 0
        endif
        clearsysmsg
    endif
    
    //    #Check close to us reporting
    //hotkey 'Next Murderer Humanoid Target'
    //wait 350
    //setvar! 'lastserial' lasttarget
    //getlabel 'lastserial' 'human_target_label'
    //if ", ***]" in 'human_target_label'
    //        wait 200
    //elseif timer 'spam close message' > 5000
    //        guild 'PK Near Tracker!'
    //        wait 500
    //        guild 'human_target_label'
    //        settimer 'spam close message' 0
    //endif
endwhile